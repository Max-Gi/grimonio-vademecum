IMPORTANTE:
- Non modificare né rigenerare il file `.env`.
- Usa esclusivamente le variabili già definite in `process.env`.
- Non creare database locali o mock: utilizza solo la tabella `eventi` già esistente in Supabase.
- Non aggiungere nuove variabili d’ambiente. Se pensi che servano, chiedi prima conferma.
- Lavora solo sul file/componente indicato, senza toccare altri file.
- Mostra solo il diff rispetto al file modificato, non l’intero progetto.
